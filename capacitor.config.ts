import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'modal-navigation',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
